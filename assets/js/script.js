function initMap() {
    var map = new google.maps.Map(document.getElementById("map"), {
        zoom: 3,
        center: {
            lat: 46.619261,
            lng: -33.134766
        }
    });
}  // this code is taken from the ealier tutorial, just using this for the moment to render map for visual layout at moment